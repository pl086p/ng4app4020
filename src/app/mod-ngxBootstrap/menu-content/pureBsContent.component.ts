import {Component, ViewEncapsulation} from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'selector-ComPureBs',
    templateUrl: './pureBsContent.component.html',
    encapsulation: ViewEncapsulation.None,
})
export class ComMenuPureBs { }
